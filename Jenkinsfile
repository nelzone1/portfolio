@Library ("shared-library") _   
pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        // Other stages here
    }
}
