@Library ("shared-library") _   
pipeline {
    agent any 
    stages {
        stage('Checkout') {
            steps {
                scmCheckout()
            }
        }
        // Other stages here
    }
}
